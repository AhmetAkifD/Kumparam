<Window x:Class="Kumparam.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Bildirim" Height="200" Width="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">

    <materialDesign:Card UniformCornerRadius="15" Margin="5" materialDesign:ElevationAssist.Elevation="Dp5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    <RowDefinition Height="Auto"/> </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{DynamicResource PrimaryHueMidBrush}" CornerRadius="15,15,0,0" Padding="15,10">
                <DockPanel>
                    <materialDesign:PackIcon Kind="BellRing" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock x:Name="TitleText" Text="Başlık" Foreground="White" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>
                </DockPanel>
            </Border>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="10">
                <TextBlock x:Name="MessageText" 
                           Text="Buraya uyarı mesajı gelecek..." 
                           TextWrapping="Wrap" 
                           FontSize="15" 
                           VerticalAlignment="Center" 
                           HorizontalAlignment="Center"
                           Foreground="#444"
                           LineHeight="22"/> </ScrollViewer>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
                <Button x:Name="CancelButton" Content="İPTAL" 
                        Style="{StaticResource MaterialDesignFlatButton}" 
                        Margin="0,0,10,0" 
                        Click="CancelButton_Click"
                        Visibility="Collapsed"/>

                <Button x:Name="OkButton" Content="TAMAM" 
                        Style="{StaticResource MaterialDesignRaisedButton}" 
                        Click="OkButton_Click"/>
            </StackPanel>
        </Grid>
    </materialDesign:Card>
</Window>
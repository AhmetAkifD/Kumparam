<UserControl x:Class="Kumparam.Pages.DashboardSubPages.AnalysisView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="1030"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <!-- Sol: Harcama Pastası (Daha Dar) -->
            <ColumnDefinition Width="1*"/> 
            <!-- Sağ: Zaman Çizelgesi (Daha Geniş) -->
            <ColumnDefinition Width="1.5*"/> 
        </Grid.ColumnDefinitions>

        <!-- 1. SOL KART: HARCAMA DAĞILIMI (PASTA) -->
        <materialDesign:Card Grid.Column="0" Margin="0,0,10,0" UniformCornerRadius="10" materialDesign:ElevationAssist.Elevation="Dp2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Başlık -->
                <StackPanel Margin="20,20,20,10">
                    <TextBlock Text="Harcama Dağılımı" Style="{StaticResource MaterialDesignHeadline6TextBlock}"/>
                    <TextBlock Text="Kategorilere göre giderler" Opacity="0.6" FontSize="12"/>
                </StackPanel>

                <!-- Grafik Konteyner (C# tarafında doldurulacak) -->
                <ContentControl x:Name="PieChartContainer" Grid.Row="1" Margin="10"/>
                
                <!-- Veri Yoksa Gösterilecek Mesaj -->
                <TextBlock x:Name="TxtNoPieData" Grid.Row="1" Text="Veri Yok" 
                           HorizontalAlignment="Center" VerticalAlignment="Center" 
                           Visibility="Collapsed" Opacity="0.5"/>
            </Grid>
        </materialDesign:Card>

        <!-- 2. SAĞ KART: GELİR / GİDER DENGESİ (SÜTUN) -->
        <materialDesign:Card Grid.Column="1" Margin="10,0,0,0" UniformCornerRadius="10" materialDesign:ElevationAssist.Elevation="Dp2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Başlık -->
                <StackPanel Margin="20,20,20,10">
                    <TextBlock Text="Son 6 Ayın Özeti" Style="{StaticResource MaterialDesignHeadline6TextBlock}"/>
                    <TextBlock Text="Aylık Gelir ve Gider Karşılaştırması" Opacity="0.6" FontSize="12"/>
                </StackPanel>

                <!-- Grafik Konteyner (C# tarafında doldurulacak) -->
                <ContentControl x:Name="BarChartContainer" Grid.Row="1" Margin="10"/>
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>